import styled from 'styled-components';

const SuitStyles = styled.span`
    font-size: 50px;

    &.red{
        color: red;
    }

    &.dark{
        color: #645f5f;
    }
`;

export default SuitStyles;